# ============================================================================
# Springware CMS — 환경 변수 설정
# ============================================================================
#
# 로컬 개발 환경 세팅 순서:
#
#   1. 이 파일을 복사하여 .env 파일 생성:
#        cp .env.example .env
#
#   2. Node.js 패키지 설치:
#        npm install
#
#   3. (DB 사용 시) Oracle Instant Client 설치 필요:
#        - 다운로드: https://www.oracle.com/database/technologies/instant-client/downloads.html
#        - Windows: Basic 패키지 압축 해제 후 PATH에 추가
#        - Mac: Homebrew로 설치 가능 (brew install instantclient-basic)
#        - oracledb 패키지가 Thick 모드로 동작하므로 반드시 필요
#
#   4. (DB 사용 시) 아래 Oracle DB 계정 정보 입력 + DB_ENABLED=true 설정
#
#   5. 개발 서버 실행:
#        npm run dev
#
# ── .env 로드 참고 ──
#   - npm run dev (Next.js): .env를 자동으로 로드함
#   - 단독 스크립트 (npx tsx *.ts 등): .env를 자동 로드하지 않음
#     → 스크립트 상단에 import 'dotenv/config'; 추가 필요
#     → 또는 실행 시: npx dotenv -- npx tsx 스크립트.ts
#
# ============================================================================

# ── AI 프로바이더 (선택) ──
# 에디터 AI 코드 생성/채팅 기능에 필요. 없으면 AI 기능만 비활성화됨.
OPENROUTER_API_KEY=
OPENAI_API_KEY=

# AI 이미지 생성 (fal.ai)
FAL_API_KEY=

# Google GenAI (웹 버전)
GEMINI_API_KEY=

# ── 파일 업로드 ──
# 에디터에서 업로드한 파일이 저장되는 경로. 기본값 그대로 사용 가능.
UPLOAD_PATH=public/uploads/
UPLOAD_URL=uploads/

# ── DB 전환 플래그 ──
# true : Oracle DB 기반 (페이지 저장/로드, 금융 컴포넌트 등)
# false: 파일 기반 폴백 (data/*.json, finance-component-data.ts)
# DB 없이 실행하려면 false로 유지
DB_ENABLED=false

# ── Oracle DB ──
# DB_ENABLED=true 일 때만 사용됨.
# Oracle Instant Client가 설치되어 있어야 함 (Thick 모드).
# 계정 정보는 팀 내부 채널로 공유.
ORACLE_USER=
ORACLE_PASSWORD=
ORACLE_HOST=
ORACLE_PORT=1521
ORACLE_SERVICE=XE
ORACLE_SCHEMA=
