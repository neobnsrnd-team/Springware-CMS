# ── AI 프로바이더 ──
OPENROUTER_API_KEY=
OPENAI_API_KEY=
FAL_API_KEY=
GEMINI_API_KEY=

# ── 파일 업로드 ──
UPLOAD_PATH=public/uploads/
UPLOAD_URL=uploads/

# ── Oracle DB ──
# Docker Hub에서 자동 pull (계정/라이선스 불필요)
# docker compose up -d 실행 후 약 2분 대기
ORACLE_USER=
ORACLE_PASSWORD=
ORACLE_HOST=localhost
ORACLE_PORT=1521
ORACLE_SERVICE=XEPDB1
